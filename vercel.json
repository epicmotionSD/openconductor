{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install --legacy-peer-deps && cd packages/shared && npm install --legacy-peer-deps && cd ../frontend && npm install --legacy-peer-deps && cd ../..",
  "outputDirectory": "packages/frontend/.next"
}
