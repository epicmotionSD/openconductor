# OpenConductor Environment Variables Template
# Copy to .env and fill in your values

# ============================================
# DATABASE CONFIGURATION
# ============================================

# Local Development (Docker PostgreSQL)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/openconductor"

# Production Supabase (Pooler URL - port 6543)
# Get from: Supabase Dashboard > Settings > Database > Connection Pooling
# PRODUCTION_POSTGRES_URL="postgresql://postgres.[project-ref]:[password]@aws-0-us-east-1.pooler.supabase.com:6543/postgres"

# Supabase MCP (Postgres) connection string
# Used by claude_desktop_config.template.json for the Supabase MCP server
SUPABASE_DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-us-east-1.pooler.supabase.com:6543/postgres"

# Direct connection (for migrations - port 5432)
# DIRECT_URL="postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres"

# ============================================
# SUPABASE REST API
# ============================================
# Get from: Supabase Dashboard > Settings > API

# Project reference (used by Supabase MCP server)
SUPABASE_PROJECT_REF="your-project-ref"

# Server-side (no prefix needed)
SUPABASE_URL="https://[project-ref].supabase.co"
SUPABASE_ANON_KEY="your-supabase-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key"

# Client-side (Next.js requires NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_SUPABASE_URL="https://[project-ref].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-supabase-anon-key"

# ============================================
# API CONFIGURATION
# ============================================

# API URL for frontend (Next.js)
NEXT_PUBLIC_API_URL="http://localhost:3002"

# For Vite-based apps (if any), use:
# VITE_API_URL="http://localhost:3002"
# VITE_SUPABASE_URL="https://[project-ref].supabase.co"
# VITE_SUPABASE_ANON_KEY="your-supabase-anon-key"

# ============================================
# EXTERNAL SERVICES
# ============================================

# GitHub API Token (for fetching repo data)
# Create at: https://github.com/settings/tokens
# Required scopes: repo, read:org
GITHUB_TOKEN="your-github-personal-access-token"

# WalletConnect Project ID (for Web3 wallet connections)
# Get from: https://cloud.walletconnect.com
# 1. Sign in with GitHub or email
# 2. Create a new project
# 3. Copy the Project ID
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID="your-walletconnect-project-id"

# Redis Cache (local Docker)
REDIS_URL="redis://localhost:6379"

# ============================================
# AUTHENTICATION (Optional)
# ============================================
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-nextauth-secret-min-32-chars"

# Google OAuth (Optional)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# ============================================
# ENVIRONMENT
# ============================================
NODE_ENV="development"

# ============================================
# FRAMEWORK NOTES
# ============================================
# Next.js: Use NEXT_PUBLIC_ prefix for client-side variables
# Vite: Use VITE_ prefix for client-side variables
# Express/Node: No prefix needed (all server-side)
#
# Current stack:
# - packages/frontend: Next.js (NEXT_PUBLIC_)
# - packages/api: Express (no prefix)
# - marketplace/: Next.js (NEXT_PUBLIC_)
