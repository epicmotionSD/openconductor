{
  "name": "openconductor",
  "version": "0.1.0",
  "description": "The control plane for AI agent systems - discover, orchestrate, and monitor MCP servers",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"cd packages/frontend && npm run dev\" \"cd packages/api && npm run dev\"",
    "build": "npm run build:shared && npm run build:api && npm run build:frontend && npm run build:cli",
    "build:shared": "cd packages/shared && npm run build",
    "build:api": "cd packages/api && npm run build",
    "build:frontend": "cd packages/frontend && npm run build",
    "build:cli": "cd packages/cli && npm run build",
    "install:all": "npm install && cd packages/shared && npm install && cd ../api && npm install && cd ../frontend && npm install && cd ../cli && npm install",
    "clean": "rm -rf node_modules packages/*/node_modules packages/*/dist packages/frontend/.next",
  "test": "jest --passWithNoTests || true"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "concurrently": "^8.2.0",
    "typescript": "^5.0.0"
  },
  "keywords": [
    "ai",
    "agents",
    "mcp",
    "orchestration",
    "model-context-protocol"
  ],
  "author": "Your Name",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/openconductor.git"
  }
}
